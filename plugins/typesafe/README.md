# TypeSafe plugin

This is the Codex plugin surface for the TypeSafe Jev `system_one` MCP tool.

Status in this repository: `CLOUD_ENDPOINT_PENDING`. The companion `.mcp.json` intentionally uses `mcp.example.invalid`; replace it only after an HTTPS deployment, OAuth configuration, and live canary have been verified. Do not put provider keys or OAuth tokens in this directory.

The `typesafe-ai` skill keeps the Choice/Noul/Score semantics and leaves final decisions with the host model. The cloud service and deployment runbook live at the repository root.
