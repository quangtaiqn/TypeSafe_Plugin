# Evidence policy

Only redacted, reproducible evidence belongs here. Do not commit provider keys, OAuth tokens, request bodies containing user data, raw logs, reviewer credentials, or screenshots with account identifiers. Local recovery snapshots and raw evidence remain ignored.

Evidence should state the surface, version, commit/ref, command or UI action, result, and boundary between mock/local and live proof. Inspector/mock output is never labeled as live host or Production evidence.
