{
  "name": "typesafe-jev-mcp",
  "version": "0.1.0",
  "description": "TypeSafe Jev System One MCP service and Codex plugin source.",
  "main": "src/index.mjs",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/index.mjs",
    "start:cloud": "node service/src/main.mjs",
    "check": "node --check src/index.mjs && node --check src/server.mjs && node --check src/schema.mjs && node --check src/typesafe-client.mjs && node --check service/src/auth.mjs && node --check service/src/limits.mjs && node --check service/src/logging.mjs && node --check service/src/http-app.mjs && node --check service/src/main.mjs && node --check scripts/recovery.mjs && node --check scripts/secret-scan.mjs",
    "test": "node --test",
    "secret-scan": "node scripts/secret-scan.mjs --root . --tracked",
    "validate:plugin": "node scripts/validate-plugin.mjs plugins/typesafe"
  },
  "keywords": [
    "mcp",
    "typesafe",
    "jev"
  ],
  "author": "Quang",
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "zod": "4.6.5"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "2.0.0",
    "@modelcontextprotocol/inspector": "2.7.0"
  },
  "private": true,
  "engines": {
    "node": ">=22.9"
  }
}
